# DirectoryContentMenuItemPlugin API

Exported Plugin Components must implements the following API

props :
```json
{
    id: <String>, // id of this plugin (must be unique for this group of plugins).
}
```
